__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.env
.env.local

# Node
node_modules/
pwa/dist/

# temp files
*.tmp
*.temp

# Local scraper output (belongs in data branch, not feature branches)
data/
data-push/
data-local/
# ...but the Android `data` package is source code, not scraper output
!android/app/src/main/java/ru/mpgu/rasp/data/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
# ignore local Claude config but keep the committed skills library
.claude/*
!.claude/skills/
.claude/settings.local.json
data_test/
data_teachers_tmp/
meta/

# local data-branch worktree for audits
.data-wt/

# scratchpad: audit dumps, downloaded source files, ad-hoc scripts
# — never checked in (source PDFs are large and republishable from mpgu.su).
scratchpad/
